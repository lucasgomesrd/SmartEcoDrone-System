import random

def coletar_dados():
    return {
        "temperatura": random.randint(20, 35),
        "umidade": random.randint(40, 90),
        "localizacao": (random.uniform(-15, -16), random.uniform(-47, -48))
    }

print(coletar_dados())
